algoritmo "funções"
var
   a, b, sum : inteiro
funcao soma(x,y: inteiro): inteiro
inicio
      retorne(x+y)
fimfuncao

inicio
      escreva("A: ")
      leia(a)
      escreva("B: ")
      leia(b)
      sum <- soma(a, b)
      escreval("Soma entre", a, " e",  b, " =" , sum)
fimalgoritmo
