algoritmo "semnome"
var
   V : vetor[1..6] de inteiro
   count : inteiro
inicio
      para count <- 1 ate 6 faca
           escreva("Digite o", count, "o. valor: ")
           leia(V[count])
      fimpara
      
      para count <- 1 ate 6 faca
           escreva("{", v[count], "}")
      fimpara
fimalgoritmo
